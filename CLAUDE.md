# MEGAFONT

캠퍼스 공공 발화 설치. 학생이 폰으로 한 줄을 쓰고 **그 형식까지 직접 정해**
벽에 올린다. 사흘 뒤 사라진다.

## 지도는 여기 없다 — `README.md`에 있다

화면 흐름(00~08), 경로(`/` · `/wall` · `/admin` · `?mock=1`), 스택, 배포,
서체 네 칸, "한곳에서 정하는 값"이 어느 파일인지. 전부 거기 있고 최신이다.
**코드를 훑기 전에 그것부터 읽는다.**

수치를 이 파일에 옮겨 적지 않는다. 값은 반드시 낡는다 — 실제로 그런 사본이
하나 있었고, 대조해 보니 무드 개수도 강조 시간도 체류 기간도 화면 개수도
전부 틀린 값을 몇 달째 주장하고 있었다. 여기엔 **어디를 보라는 포인터**와
**지켜야 할 규칙**만 둔다.

## 절대원칙

**편집권은 발화자에게 있다. 시스템은 형식을 추천하지 않는다.**
음성·AI는 입력으로만 쓰고 톤 추천에 쓰지 않는다. 무작위는 색과 그래픽까지,
나머지는 전부 사람이 명시적으로 고른다. 기능을 더하기 전에 이걸 먼저 본다.
(출처: `design/direction-v0.md`)

## 스타일

**색·간격·글자 크기·라운드는 `src/styles/tokens.css`의 변수만 쓴다.**
하드코딩된 hex나 px를 넣지 않는다. 필요한 값이 없으면 토큰을 먼저 만들고,
기존 사다리에 없는 새 값이면 **묻는다.**

예외가 하나 있다: **메시지의 색은 토큰이 아니다.** 참여자가 고르는 내용이라
`src/lib/palettes-v2.ts`가 데이터로 들고 있다. 토큰 감사를 할 때
`palettes*.ts`를 세면 오탐만 나온다.

토큰은 한 파일뿐이다. `app.css`나 `global.css`에 `:root`를 만들지 않는다 —
두 곳에 두면 그중 하나가 반드시 죽는다(그래서 합쳤다).

움직임도 같다. **이징·시간은 `tokens.css`의 `--ease-*`·`--t-*`에서 고른다.**
모션 스킬(Emil Kowalski, 전역에 있다)은 규칙을 주지 값을 주지 않는다 — 제
커브나 숫자를 들고 오면 토큰으로 바꿔 넣거나 **묻는다.** 그 스킬의 시간 상한
("UI는 300ms 이하")은 **손으로 조작하는 UI**에만 해당한다. 벽의 맥동·도킹·
표류는 UI가 아니라 작품이고, 스킬 자신의 표에서도 "드묾·설명용 — 길어도
됨"으로 예외다. 자간·행간·광학 크기는 스킬이 아니라 내가 정한다.

**틀은 선이 긋고, 면은 내용이 갖는다.** 레퍼런스 카드에서 같은 축('차이')이
네 장 쌓여 규칙으로 올렸다(2026-09-25, `design/references.md` R05·R08~R10, 경계는 R07).

- 영역의 경계는 1px 선이다. 채운 둥근 상자를 띄워 나누지 않는다
- 사각 버튼·입력 컨트롤의 모서리는 3px(`--r-control`, 2026-09-25 합의). 원형 아이콘
  버튼과 점은 원으로 둔다. 알약 모양은 쓰지 않는다
- 여백은 값이 아니라 역할 이름으로 쓴다 — `tokens.css`의 '여백의 역할'
- 누르는 것은 글자와 선으로 그린다. 주 행동만 선으로 된 칸을 갖고, 맨 글자로
  내리지 않는다 — 처음 온 사람이 다음 길을 찾아야 한다
- 강조색은 한 화면에 한 곳
- 축은 선의 방향으로 그 성격을 말한다(R05)

메시지의 색·구름은 이 규칙 밖이다 — 참여자가 고른 내용이다. 화면 적용은
2026-09-25부터 차례로 하고 있다. 둥근 곳이 남아 있으면 미적용이지 예외가 아니다.

## DESIGN.md와 oh-my-design

루트 `DESIGN.md`가 이 앱의 디자인 계약이다 — 참여자 폰 화면 하나만 다루고
`/wall`과 `/admin`은 범위 밖이라고 거기 적혀 있다. oh-my-design의 Core v2
형식이고 지금 검증을 통과한다. 손댔으면 다시 통과하는지 본다:

```bash
npx oh-my-design-cli@latest design-md validate DESIGN.md
```

스킬 묶음(`omd:*`)은 **전역**(`~/.claude/`)에 있다. 이 저장소에는 아무것도
설치하지 않았다 — `.claude/`에 `omd-`로 시작하는 파일이 생기면 잘못 깔린 것이다.

메가폰트에서 OmD를 쓸 때 지키는 선이 넷이다.

- **값이 사는 곳은 여전히 `tokens.css` 하나다.** `DESIGN.md`는 값을 들고 있는
  문서가 아니라 **왜 그 값인지**를 적는 문서다. 새 토큰 파일도 새 `:root`도
  만들지 않는다. 위 "스타일"이 그대로 이긴다.
- **타입 스케일과 자간은 OmD가 손대지 않는다.**
- **절대원칙이 위다.** OmD는 만드는 쪽에 레퍼런스를 추천하는 도구지 학생에게
  톤을 추천하는 장치가 아니다. `omd:init`·`omd:apply`가 제안한 것이 참여자
  화면의 무드·서체·말투를 대신 고르게 되면 그건 원칙 위반이다.
- **OmD 감사는 `audit-a11y.mjs`를 대체하지 않는다.** `omd:slop-audit`가
  `palettes-v2.ts`의 hex를 하드코딩으로 잡으면 오탐이다(위 예외 참조).

## 코드를 바꿨으면

`npm run dev`를 띄운 채로:

```bash
npx tsc --noEmit && npm run build
node scripts/audit-a11y.mjs     # 위반 0이 기준선. 한 건이라도 나오면 회귀다
node scripts/e2e-dock.mjs       # 폰↔벽 왕복. 실제 Firestore에 쓰고 지운다
```

CSS·토큰을 건드렸으면 `scripts/verify-tokens.mjs`로 전후 계산값을 대조한다.
"보기엔 같은데"는 증명이 아니다.

`.claude/settings.json`에서 **`.env` 읽기를 막지 않는다.** 쓰기만 막혀 있다.
`e2e-dock.mjs`가 거기서 Firestore 키를 얻어 **테스트 글을 지우기** 때문이다 —
읽기를 막으면 테스트가 남긴 글이 사흘 동안 벽에 떠 있게 된다.

## 일하는 방식

- **코드를 읽고 짐작하지 말고 열어서 본다.** `scripts/lib/drive.mjs`가
  브라우저와 화면 이동을 들고 있다. 자세한 건 `peek` 스킬.
- **눈으로 판정할 수 있는 것도 잰다.** 값을 고를 땐 한 장에 나란히 놓고
  고른다 — `tuning-grid` 스킬.
- **배포는 `deploy` 스킬로.** `main`에 push하면 학생들이 쓰는 설치물에
  곧바로 나간다. push 전에 반드시 확인받는다.
- **화면 캡처(`shoot-screens.mjs`)는 시킬 때만 돌린다.** 바뀔 때마다
  자동으로 갱신하지 않는다.
- **탐색은 서브에이전트에 맡기고 결론만 받아도 된다.** "이 클래스가 어디
  어디서 쓰이나" 같이 여러 파일을 훑어야 하는 일이 그렇다. 탐색 과정이
  이 대화에 쌓이지 않는 편이 낫다.
- **이름은 발명하지 말고 찾는다.** 컴포넌트를 뭐라 부를지 정할 때
  [component.gallery](https://component.gallery/components/)에서 실제 디자인
  시스템들이 그걸 뭐라 부르는지 먼저 본다. 별칭이 여럿이면 그 물건이 아직
  이름을 못 찾았다는 뜻이다. **채택 수는 근거가 아니다** — 그 사이트 주인이
  직접 달아 둔 경고다. 히어로는 다들 당연하게 치지만 실제로 컴포넌트로 가진
  시스템은 한 줌뿐이다. 당연해 보이는 것이 실제로는 안 당연하다.

## 커밋

메시지는 **무엇을 바꿨는지가 아니라 왜 그래야 했는지**를 적는다.
문제 → 증거(수치) → 선택 → 안 한 것. 기존 메시지 몇 개를 읽고 결을 맞춘다.

## 아직 안 된 것

`README.md` 맨 아래에 있다. 도킹 실기 확인, 성격별 축 개편
(당당함의 말투=GLAT 등), 출시 전 보안.

서체 넷은 2026-09-20에 전부 갈렸고 '발랄한'은 '유머있는'이 되었다 —
'당당한'과 골격이 겹치던 문제는 그때 없어졌다.
